import React from 'react'
import { Form, Button } from 'react-bootstrap';

export default function Headersignup() {
  return (
    <div>
      <input
        type="email"
        placeholder="email"
      />
    </div>
  )
}
